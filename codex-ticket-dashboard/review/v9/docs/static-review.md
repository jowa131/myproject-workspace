# 자동 기록 연결 정적 코드 검토

판정: 설계 보완 필요. 실제 Desktop 누락의 직접 원인은 미확정입니다. 추가 모델 시험·보조 작업자·앱 조작·pytest·빌드 없이 현재 코드를 읽었습니다. 전체 5개 분야의 review-work 승인이나 실행 검증 통과를 주장하지 않습니다. 사용자 비용 제한에 따라 목표 충족과 연결 계약만 검토했습니다.

- 메모 파일 읽기는 제품 설계의 기능이나 수집원이 아닙니다. 조사자가 선택한 시험용 요청이었고, 이전 두 요청은 별도 모델 응답을 발생시켰습니다. 이를 반복한 비용 통제 실패를 제품 요구로 정당화하지 않습니다.
- 관측과 업무 등록은 별도 경로입니다. lifecycle/command.py:34-48은 관측을 접수하고 RECORDING_GUIDANCE를 반환합니다. recording_guidance.py:5-21은 모델에게 preflight/task/summary/status 기록을 호출하도록 요구합니다. ingest/projector.py:63-64와 ingest/lifecycle_projection.py:23-108은 관측 투영을 처리하며, lifecycle_projection.py:111-129는 이미 존재하는 ticket_events/tickets와 연결할 뿐 새 업무 기록을 만들지 않습니다. 따라서 Hook 관측 성공만으로 자동 업무 등록 성공이 되지 않으며 모델의 후속 호출이 필수입니다.
- MCP는 시작 관측 누락의 독립 복구 경로가 아닙니다. mcp/official_context.py:125-146은 같은 thread/turn 관측 후보가 없으면 OBSERVER_CONTEXT_MISSING으로 거절합니다. lifecycle/initial_preflight.py:45-57 역시 DB에 반영된 UserPromptSubmit 관측이 없으면 최초 기록 보완 대상에서 제외합니다. 이는 식별/권한 보호 조건이므로 삭제해 통과시키면 안 됩니다. 시작 관측이 없는 현재 상황에서 안내 문구나 재입력만 강화해도 해결되지 않는 구조입니다.
- 보완은 추가 모델 실행에 의존합니다. lifecycle/stop_observer.py:64-89에서 조건부 1회 claim을 소비하고 initial_preflight.py:154-179의 decision:block/reason을 반환합니다. 근거 없는 재시도는 허용되지 않으며, 보완이 발생하면 모델 실행 비용이 생깁니다. 실행 경로가 확인되지 않은 상태에서 보완 코드를 계속 확장하면 비용과 복잡도만 증가할 수 있습니다.

권고: 먼저 지원되는 관측 입력에서 구조화 사건 하나가 들어오는 연결 계약과 실행 결과 확인 수단을 확정해야 합니다. 이후 기본 활동 등록을 코드가 책임질 범위와 모델이 의미/결과를 보강할 범위를 분리하는 설계안을 검토해야 합니다. 기존 spool/수집기/DB/조회 화면을 재사용하고, 자동 완료나 ID/결과 추정은 도입하지 않습니다. 이 권고가 현재 호스트 문제의 검증된 해결책이라는 뜻은 아닙니다.

이번에는 제품 코드/설정/서비스를 변경하지 않았습니다. 실제 Hook 실행 결과를 확보하지 않은 추가 유료 일반 요청이나 전체 시험은 진행하지 않습니다. 이전 권한 부여를 불확실한 반복 실험 허용으로 해석하지 않습니다.
## 다음 단계 판단: 2026-09-09

- 입력 계약의 주요 필드명 대조에서는 수정할 불일치를 찾지 못했습니다. 보관된 Codex schema.rs의 UserPromptSubmitCommandInput(567-582)은 session_id/turn_id/cwd/hook_event_name과 선택 agent_id를 보내며, 현재 normalization.py:45-60은 해당 필드를 받습니다. Stop의 stop_hook_active도 수신 필드와 일치합니다. 이는 소스 계약 대조이며 설치 Desktop이 실제 같은 값을 보냈다는 증거는 아닙니다.
- 관측은 admission.py:53-62에서 OBSERVER/HOOK 권한이며 ticket_id/work_item_id가 없습니다. 현재 계약을 유지한 채 모델의 의미 해석 없이 업무 티켓을 직접 생성하도록 바꾸는 것은 단순 버그 수정이 아닙니다. 앞선 책임 분리 권고는 설계 검토안으로 남기고, 입구 고장의 해결책으로 확정하지 않습니다.
- 다음 작업 범위는 실제 Desktop의 Hook 선택/실행과 수신기 진입 사이의 연결 계약 하나입니다. 필요한 정보는 선택한 정의 key/source path, 실행 여부, 명명된 실행 결과입니다. 원문 대화나 메모 파일 읽기가 필요하지 않습니다.
- 새 정보 없이 앱 입력·활성 토글·명령 비교를 다시 실행하지 않습니다. 현재 확인한 불일치가 없으므로 수신기나 저장 코드를 추측 수정하지 않습니다. 후속 수정은 이 연결에서 확인된 결함 하나에만 한정합니다.
- AGENTS.md에 전체 진단 10분 한도, 시험용 모델 요청/작업자 기본0, 새 근거 없는 재시험 금지, 잘못된 재현의 근거 무효화, 모델 비용 라우팅을 기록했습니다. 이 규칙 추가가 자동 기록 문제 해결을 뜻하지 않습니다.
