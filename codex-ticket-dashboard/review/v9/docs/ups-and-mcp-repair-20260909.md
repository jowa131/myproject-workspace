# 제출 Hook과 MCP 입력 경계 교정

2026-09-09 KST. 제품 목표는 일반 Codex 작업을 별도 기록 지시 없이 관측하고 티켓·작업·상태·요약으로 연결하는 것입니다. 2026-09-10 00:50 KST에 기존 운영 데이터로 로컬 수집기 기동을 확인했습니다. 주 프로젝트 연결과 정식 릴리즈 수락은 각각 최종 연결 영수증과 남은 수락 조건으로 구분합니다.

## 확인된 원인과 수정

1. 기존 UPS 3초 설정에서는 실제 셸 진입만 기록되고 자식 종료·UPS 접수 기록이 없었습니다. 5초 설정과 단계 기록을 적용한 실제 요청에서는 셸 진입부터 종료까지 2.787초, Python 임포트 1.923초, CLI 처리 0.166초로 정상 종료했습니다. Host에서 셸을 시작하는 시간까지 고려하면 3초 상한의 여유가 부족합니다. 설계 §11.1은 SessionStart 2초·SessionEnd 3초를 명시하지만 UPS·Stop에 3초 상한을 요구하지 않습니다. UPS·Stop 설정만 5초로 고쳤으며 임시 단계 실행기는 최종 활성 정의에서 제거했습니다.
2. 실제 자동 등록 요청은 티켓·작업 항목·요약을 만들었지만 Git 기록에 `MODEL_POLICY_CONTEXT_CONFLICT`가 발생했습니다. 모델이 서버 소유 필드 `authority_source`를 직접 채운 것이 원인이었습니다. 공식 문맥을 결속하는 MCP 모드에서 서버 소유 필드를 공개 입력 스키마에서도 제거했습니다. 호출자의 실제 결과·적용 선언은 유지하고, 명시적으로 충돌하는 값의 거부도 유지합니다.

## 실제 증거

- Desktop 일반 요청 `01a0868c-63b2-7210-9a6a-7551150adc72`: UPS와 Stop 모두 `ADMISSION / RECORDED`; 티켓 생성 1, 작업 생성·갱신 각 1, 상태 변경 2, 턴 요약 1, Wiki 결과 1로 업무 사건 7개가 반영됐습니다. 최종 티켓은 Git 정책 충돌과 Wiki 근거 미완료를 반영한 `BLOCKED`이며 제품 완료 증거가 아닙니다.
- 수정본 집중 테스트: 공개 스키마·생략 필드 결속·충돌 거부 3개, 보호 실행기 1개, Hook 설정 1개가 통과했습니다. 임시 진단 도구의 합성 테스트 3개는 제품 수락 사례와 별도입니다. 이전 잠금·초기 로딩·정책 갱신 테스트를 반복하지 않았습니다.
- 보호 MCP 수정본의 실제 stdio 초기화·도구 목록 조회가 통과했습니다. 도구 7개, 서버 소유 필드 비노출, 호출자 결과 필드 보존을 확인했으며 모델·업무 기록 호출은 0개였습니다.
- MCP 소스·실행기 100개 파일, 618293바이트와 프로젝트 연결 설정 2개를 적용했습니다. 기존 보호 의존성·수집기·DB·Hook 제품 소스를 재사용했습니다. 의존성 재복사 0바이트, 수집기 재시작 0회입니다.
- 23:50:53 KST 공식 Codex UI에서 최종 UPS·Stop 명령과 5초 상한을 검토하고 신뢰·활성을 확인했습니다. 기존 다른 4개 Hook은 유지했습니다.
- 수정 소스는 `codex/v9-core-fix-20260909`, `fa012bc42024b03c1825b34ae1de5be17065959e`에 게시했고 원격 일치를 확인했습니다. 원래 v9 기준 커밋은 보존했습니다.

## 남은 경계

- 수정된 MCP 입력 스키마를 사용한 CLI는 티켓 생성·작업 생성·진행 상태 변경 3건까지 접수한 뒤 exit 1로 끝났습니다. 티켓은 IN_PROGRESS이며 종료 요약·Git·Wiki 완료 기록과 Stop 관측은 없습니다. 모델 작업의 최종 종료 원인은 확보된 기록만으로 확정하지 못했습니다. 이 사례는 전체 성공이 아닙니다.
- 새 Desktop·재개/압축·실제 하위 작업 등 수락표의 미확인 조건을 기존 증거와 대조해야 합니다. 한 성공 사례를 전체 조건 통과로 계산하지 않습니다.
- 확인용 설정 복구와 서비스 종료 후, 기존 논리 프로젝트의 운영 등록·주 프로젝트 연결을 적용해야 합니다.
- 최종 사용자 수락과 PC 재부팅 시점은 대신 결정하지 않습니다. 모바일·Chrome 확대의 기존 미실행/보류도 유지합니다.

## 총괄 오류 정정

초기 진행 출력의 23:04~23:54 표기 일부는 UTC에 10시간을 더한 오류였으며 실제 KST는 22:04~22:54입니다. UTC 영수증과 `DateTimeOffset`으로 생성한 기계 기록은 영향을 받지 않았습니다. 22:55 KST에 사용자에게 정정했습니다.

조회 과정에서 확인하지 않은 DB 테이블·열 이름을 사용해 읽기 전용 조회가 실패했고, 실행 파일을 텍스트로 읽어 불필요한 바이너리 출력을 발생시킨 실수도 있었습니다. DB는 실제 스키마의 이름을 확인한 뒤 조회하고, 실행 파일은 형식 확인 없이 텍스트 도구로 열지 않습니다. 이 오류를 사용자 지시 부족이나 제품 테스트 실패로 돌리지 않습니다.

근거: `.omo/evidence/ups-entry-repair-20260909/`의 `actual-python-phase-projection.json`, `python-phase-decision.md`, `component-release-ready.json`, `installed-mcp-schema.json`, `component-product-install.json`, `component-host-install.json`, `cli-actual-status.json`.

## 2026-09-10 후속 기록

CLI의 Wiki 갱신에 사용한 git apply가 기존 혼합 LF/CRLF를 CRLF로 바꾸었습니다. 수정 전 264470바이트의 SHA256은 `8b19237714882daad9491ccdafbffe0a7edfba786fd048f8262eabb5152e1248`이며 원본 바이트 복원은 확인하지 못했습니다. 현재 파일을 증거 영역에 보존했고, 추가 줄끝 재구성은 제품 결정에 기여하지 않아 중단했습니다. 이 기록 작업의 실패를 제품 코드 결함이나 사용자 지시 부족으로 돌리지 않습니다.

운영 후보 준비는 기존 DB의 프로젝트 version 2, policy 0행, PLANNED 티켓 1건을 읽기 전용으로 확인했고, 등록 갱신 사전 검사와 설치 명세 검사가 통과했습니다. 실제 활성화 결과는 `.omo/evidence/operational-cutover-20260910/`의 최종 영수증을 따르며 준비만으로 활성화나 정식 릴리즈를 주장하지 않습니다.

### 운영 전환의 실제 결과 (2026-09-10 00:50 KST)

- QA config를 원본 SHA256·ACL로 복원하고 원래 없던 Hook 파일을 제거했습니다. QA PID 50692 종료·포트 해제를 확인했습니다.
- 운영 core 설치 마지막 config 교체에서 inherited DACL 9개가 0개로 바뀌어 설치기가 차단했습니다. 저장된 Access 섹션만 복원하고 소유자·그룹·전체 DACL의 원본 일치와 여섯 파일의 설치 SHA256을 확인했습니다. 최초 설치 실패와 수동 복구 영수증은 구분해 보존했습니다.
- 운영 프로세스 PID 36160, launcher 5984를 한 번 시작했습니다. 첫 25초 확인에서는 READY를 얻지 못했으나 같은 프로세스의 기동 완료 로그 뒤 HTTP 200/READY 및 Cache-Control: no-store를 확인했습니다. 추가 기동은 하지 않았습니다.
- 기존 DB는 프로젝트 version 2에서 3으로 갱신되고 policy 1행이 생성됐으며 기존 PLANNED 티켓 1건이 유지됐습니다. 새 DB로 대체하거나 이전 기록을 삭제하지 않았습니다.
- 근거: `.omo/evidence/operational-cutover-20260910/{qa-restored-and-stopped,acl-failure-diagnosis,core-apply-recovery,installed-runtime-preflight,collector-ready}.json`.

### 최종 연결 상태 (2026-09-10 00:58 KST)

주 프로젝트의 `.codex/config.toml`과 `.codex/hooks.json` 적용은 자동 승인 검토에서 두 차례 거절됐으며 실행되지 않았습니다. 재심사에는 AGENTS.md 147~149행의 최신 자율 진행 범위를 제시했지만 승인되지 않았습니다. 전역 설정이나 신뢰 DB를 수정하는 우회는 하지 않았습니다. 운영 수집기 READY와 실제 Host 연결·제품 완료는 별도 상태입니다.

설치기 Apply 경로에는 기존 소유자·그룹의 일치를 확인한 뒤 저장된 Access 섹션만 복원하고 전체 권한을 대조하는 최소 수정을 추가했습니다. 첫 회귀 fixture는 원래 권한 저장 후 부모 권한을 바꾸어 비교 기준이 달라진 오류가 있었습니다. 이 부모 변경을 제거하고 빈 DACL 손실을 재현한 집중 테스트 1개가 0.519초에 통과했습니다. 기존 권한 비교와 오류 차단은 유지했습니다. 실제 운영 config의 원본 권한 복구는 별도 영수증으로 확인했으며, 수정된 설치기로 주 프로젝트 Host Apply를 실행한 증거는 없습니다.

남은 작업은 (1) 차단된 현재 프로젝트 연결 두 파일 적용과 공식 신뢰 확인, (2) CLI 종료 요약·Git·Wiki 마무리를 포함한 실제 업무 연결 확인, (3) 기존 수락표의 재개/압축·하위 작업 관계 등 미확인 항목의 판정, (4) 최종 사용자 수락입니다. 자동 시작·백필·모바일·Chrome 200% 확대 범위를 추가하지 않았습니다.
